namespace ProfitRobots.FXTS2LuaExecuter
{
    public class ToolCreationSrategy
    {
        /// <summary>
        /// Sets max click count for the process of creating a tool.
        /// </summary>
        /// <param name="count"></param>
        public void setMaxClickCount(int count)
        {

        }

        /// <summary>
        /// Sets request for dialog on setting parameters after finishing creation pattern.
        /// </summary>
        /// <param name="need"></param>
        public void setNeedParamsAfterPattern(bool need)
        {

        }

        /// <summary>
        /// Sets the type of the creation pattern.
        /// </summary>
        /// <param name="pattern"></param>
        public void setPattern(int pattern)
        {

        }
    }
}
