namespace ProfitRobots.FXTS2LuaExecuter
{
    class BarStream : TickStream
    {
        //close The stream of close prices.
        //high The stream of high prices.
        //low The stream of low prices.
        //median The stream of median prices.
        //open The stream of open prices.
        //typical The stream of typical prices.
        //volume The stream of volumes.
        //weighted The stream of weighted prices.
        //getAll The method returns the Open, High, Low, and Close values of the specified period.
        //supportsVolume The method checks whether the stream supports the trading volume.
    }
}
